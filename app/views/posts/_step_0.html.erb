<%= f.hidden_field :step, :value => '0' %>

<div class="field">
	<%= f.label :name %><br />
	<% if @post.new_record? %>
		<%= f.text_field :name, :value => current_user.name, :title => "Field help text goes here" %>
	<% else %>
		<%= f.text_field :name, :title => "Field help text goes here" %>
	<% end %></div>
<div class="field">
	<%= f.label :phone_number %><br />
	<%= f.text_field :phone_number %>
</div>
