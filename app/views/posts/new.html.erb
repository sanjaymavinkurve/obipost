<%= render "steps_progress", :post => @post%>

<%= form_for @post, :html => {:multipart => true} do |f| %>
	<%= render "shared/error_messages", :object => f.object %>
	<%= render "step_#{@post.current_step}", :f => f, :post => @post %>

	<% if @post.existing_post && @post.completed < 0 %>
		<%= f.submit "Save" %>
	<% else %>
		<%= f.submit "Next" %>
	<% end %>

<% end %>